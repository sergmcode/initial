import React, { FC } from 'react'
import { RootPage } from './root'

interface Props {
  
}

export const HomePage: FC = (props: Props) => {
  return (
    <RootPage>
        Home page
    </RootPage>
  )
}
